spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/audit_trails
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true